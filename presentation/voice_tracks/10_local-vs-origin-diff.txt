So, David is taking stock. On screen, OMC is summarizing what exists locally and what is already on origin slash main. The Reference docs that capture the Red Hat OpenShift AI cluster, the serving runtimes, the model deployments, the inference endpoints, are on disk. The sample app code and manifests are on disk. But not all of it is pushed yet. This is a checkpoint. It tells David what he needs to commit to make the demo reproducible. The next push will bring local and origin into alignment, so the work that follows can be reviewed by anyone. Argo CD watching the repo. A teammate cloning fresh. A viewer of this presentation reading the source.
