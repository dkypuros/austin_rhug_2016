So, OMC has deployed the sample chat app to the cluster. The namespace is composer dash ai dash apps dash demo. It is intentionally separate from the live composer dash ai dash apps namespace, so anything we do here doesn't touch the existing workloads. The harness has applied the Namespace, the sample dash chat dash code ConfigMap with the server dot p y source, the l l m dash credentials Secret with the NVIDIA API key, the Deployment, the Service, and the Route. Then OMC verifies. It hits the route at sample dash chat dash composer dash ai dash apps dash demo dot apps. The slash info endpoint returns the backend URL pointing at integrate dot api dot nvidia dot com slash v 1, with model meta slash llama 3 point 1 8 b instruct. Then it sends a real POST to slash chat. The prompt: in one sentence, what is Red Hat OpenShift AI? Llama 3 point 1 answers with a real sentence. The app is talking to NVIDIA's hosted model, served from inside the cluster, end to end.
