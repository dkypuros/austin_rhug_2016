So, here is the repo on GitHub. dkypuros slash austin underscore rhug underscore 2016. It is private at this point. David made it public later for GitHub Pages publishing. You can see the file tree at a glance. apps slash hello dash world is the initial sample. The docs folder has access dot m d and gh dash cli dash usage dot m d. Those are the runbooks the harness reviewed earlier. gitops slash argocd holds the Argo CD application and project manifests. The tekton folder holds the validation pipeline. The dot env underscore example files, three of them for NVIDIA, gh dash cli, and openshift dash lab, are the safe templates. Real values are local only, never committed. README dot m d and CLAUDE dot m d are at the root. CLAUDE dot m d is the instruction file the harness reads on every run. The Activity panel shows a single commit by David's harness. Establish a safe OpenShift GitOps harness. This is what an hour of OMC work and David's direction looks like, committed.
