David has now invoked OMC, the Oh My Claude harness. The first thing he asks it to do is a security review of the docs folder, before any code is written. OMC reads docs slash access dot m d and docs slash gh dash cli dash usage dot m d. It produces a structured findings report. It flags that the dot gitignore protection is too narrow. It flags that an O C client URL points at a Linux binary on a Mac. It flags that the gh run view documentation assumes interactive use. None of these are critical. They are guardrails. David's job here is to set the bar: no real secrets, no broken assumptions in the docs. The harness produces a clean review and David approves it. Then the harness can continue. This is the rhythm of the next several clips. David navigates and asks why. The harness builds and shows the work. Both run in their lane.
