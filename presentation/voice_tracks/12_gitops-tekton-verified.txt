So, the GitOps pipeline is live. On screen, the deployment is confirmed. The Argo CD application, sample dash app, shows Synced and Healthy. The Tekton PipelineRun, sample dash app dash c i, completed Succeeded. OpenShift BuildConfigs for the backend and frontend ran. From this point forward, every change goes through git. Argo reconciles. Direct edits get reverted. The pipeline owns the deployment.
