So, David is setting up his workspace. The terminal is split into two panes inside the project folder, austin underscore rhug underscore 2026. On the left, the harness, OMC, will run. On the right is space for inspection and verification. Both panes are empty right now. He runs p w d to confirm he is in the right place. There is no code yet. No commits. Nothing on GitHub. This is the calm before the build. David's role here is to navigate. He decides what gets built, what model gets used, and which platform it lands on. The harnesses, OMC and OMX, will write the code. Before any harness is invoked, the foundation has to be clean. From this empty terminal, the next clip is where the security review begins.
