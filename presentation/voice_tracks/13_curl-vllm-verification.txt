So, David is verifying the in cluster v L L M endpoint directly. He runs curl against vllm dash composer dash ai dash apps dot apps cluster, slash v 1 slash chat slash completions, using the same OpenAI compatible JSON body the sample app uses. The response comes back HTTP 200. The model field in the JSON is vllm. That is the alias the in cluster predictor registered at deploy time. The reply text is from Granite 3 point 0 8 b instruct, served by KServe on the GPU worker node. He also hits slash v 1 slash models. It returns a single entry with id vllm, max model length 4096, owned by vllm. This confirms two things. The in cluster Granite model is real and reachable. And it speaks the same OpenAI contract as NVIDIA's hosted endpoint. So the same client code, with three different env values, can talk to either. The next clip shows what that swap looks like in the actual demo.
