# RHOAI Reference

Reference notes captured from the live RHOAI cluster (`cluster-nhsxz`, project `composer-ai-apps`). Pure reference — no actions to take here.

- [`serving-runtimes.md`](./serving-runtimes.md) — pre-installed serving runtimes and which one is in use.
- [`model-deployments.md`](./model-deployments.md) — the `vllm` Model deployment and its underlying `InferenceService`.
- [`inference-endpoints.md`](./inference-endpoints.md) — internal vs external endpoints exposed by the Model deployment, plus an off-cluster smoke test.
